#include<stdio.h>
int main()
{
    int num;
    printf("enter the num");
    scanf("%d",&num);
    if(num==1)
    {printf("the value is 1");}
    else if (num==2)
    {printf("the value is 2");}
    else if (num==3)
    {printf("the value is 3");}
    else {
        printf(" the value is not 1,2,3");
    }

}